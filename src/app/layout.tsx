import type { Metadata } from 'next';
import Link from 'next/link';
import './globals.css';
import { appOrigin } from '@/lib/config';
export const metadata: Metadata = { metadataBase: new URL(appOrigin()), title: { default: 'Pickside — Two choices. One you.', template: '%s | Pickside' }, description: 'Settle the debate. Create a matchup, share it with friends, and pick a side.' };
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="en"><body><a className="skip-link" href="#main">Skip to content</a><header className="site-header"><Link href="/" className="brand" aria-label="Pickside home"><span className="brand-symbol" aria-hidden="true"><i/><i/></span>pickside<span className="brand-period">.</span></Link><nav className="header-actions" aria-label="Main navigation"><Link href="/my-matchups" className="account-link">My Matchups</Link><Link href="/create" className="header-create"><span className="header-create-full">Create a matchup</span><span className="header-create-short">Create</span><span className="header-create-arrow" aria-hidden="true">＋</span></Link></nav></header><main id="main">{children}</main><footer className="site-footer"><span>Small choices. Big opinions.</span><span>Made for a little friendly disagreement. <span className="footer-star" aria-hidden="true">✳</span></span></footer></body></html>; }
